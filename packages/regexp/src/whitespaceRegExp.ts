/**
 * A RegExp that matches whitespaces
 *
 * @example
 * Match whitespace
 * ```ts
 * const b = whitespaceRegExp.test(" ");
 * console.log(b); // true
 * ```
 *
 */
export const whitespaceRegExp = /\s/;
