{
  "name": "@benneq/ts-utils",
  "version": "1.0.0",
  "description": "TypeScript Utilities",
  "homepage": "/ts-utils",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "tsc --build",
    "build:ci": "run-s build",
    "build:clean": "tsc --build --clean",
    "build:pages": "run-s docs test:coverage",
    "ci": "cross-env NODE_ENV=production run-s clean build:ci test:ci",
    "clean": "run-s build:clean docs:clean test:clean",
    "compile": "tsc --build --watch --incremental",
    "docs": "typedoc",
    "docs:clean": "rimraf ./pages/docs",
    "lint": "eslint \"packages/**/*.ts\"",
    "lint:fix": "eslint \"packages/**/*.ts\" --fix",
    "prettify": "prettier --write .",
    "test": "cross-env jest",
    "test:ci": "jest --ci",
    "test:clean": "rimraf ./pages/coverage",
    "test:clearCache": "jest --clearCache",
    "test:coverage": "jest --coverage",
    "test:watch": "cross-env jest --watch"
  },
  "devDependencies": {
    "@swc/jest": "^0.2.23",
    "@types/eslint": "^8.4.9",
    "@types/jest": "^29.2.1",
    "@typescript-eslint/eslint-plugin": "^5.42.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.2.2",
    "npm-run-all2": "^6.0.2",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.1",
    "typedoc": "^0.23.19",
    "typedoc-plugin-mdn-links": "^2.0.0",
    "typedoc-plugin-resolve-crossmodule-references": "^0.3.2",
    "typescript": "^4.8.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
